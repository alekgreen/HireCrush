services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: interview-app:latest
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - interview_data:/app/data
    restart: unless-stopped

volumes:
  interview_data:
